apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy

metadata:
  name: linkding
  namespace: flux-system

spec:
  imageRepositoryRef:
    name: linkding

  policy:
    semver:
      range: "1.x"
